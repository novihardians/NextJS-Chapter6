export default function Page() {
    return <p>Dashboard menu of Emails Page not showing up because there is no default layout in NextJS for Emails. 
        You must create it manually on app/ui/dashboard/nav-links.tsx file.
    </p>;
}